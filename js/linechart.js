function CreateLineChart(dataArray, selector) {

    var data = [];
    dataArray.forEach( function(d) {
        if (d.country === "Brazil") {
            data.push(d);
      }
    });

//    console.log(dataNest);


}
